{
  "name": "@odyn/devkit",
  "version": "0.1.0",
  "description": "Development instruments, never shipped: a grid overlay that draws the site's own layout tokens, a tune panel behind ?tune=<name>, and a frame and long-task probe behind ?perf.",
  "type": "module",
  "private": true,
  "exports": {
    ".": "./src/index.ts"
  }
}
