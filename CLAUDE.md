@AGENTS.md

Claude-specific notes:

- Do not build or deploy after every edit. Run `bun run validate` when a change is complete, and deploy only when asked.
- Reference code as `path:line`. Keep messages short and lead with the outcome.
- Never mention or attribute AI assistance in commits, pull requests or docs.
