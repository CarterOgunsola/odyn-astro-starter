# Migrations

One note per starter major, for projects born from the previous one. What moved, what was removed, what to run. A project reads the note for every major between its `starter.ref` and the current tag, in order, and cherry-picks by hand. There is no sync tool, on purpose (`docs/decisions/020`).

No majors yet. The first note will be `1.0.md`.
