# src/assets

Files that go through Astro's pipeline (hashed, sized) rather than `public/`.

`fonts/` holds the two Geist variable files the Fonts API serves through the local provider (decision 007, `astro.config.ts`). Images belong here too once a page places one (decision 028).
